## Codebase Patterns
- Models use SQLAlchemy `DeclarativeBase` with `Mapped` type annotations and UUID string PKs
- No Alembic migrations — new tables auto-created by `Base.metadata.create_all()`, new columns added via idempotent `ALTER TABLE` in `init_db()`
- All models must be imported in `init_db()` (database.py) AND exported from `models/__init__.py`
- Use `IF NOT EXISTS` pattern (try/except) for column migrations in `init_db()`
- JSON columns use `sqlalchemy.JSON` with `default=list` or `default=dict`
- Timestamps use `DateTime` with `default=lambda: datetime.now(timezone.utc)`
- Run `python -m py_compile` from `backend/` dir for quick typecheck; use `python -c "from app.models.X import Y"` to verify imports
- Git operations must be run from repo root `/mnt/c/Users/dobec/actions/`
- Backend Python runs from `/mnt/c/Users/dobec/actions/backend/`

---

## 2026-02-26 - US-001
- Implemented AgentIteration model in `backend/app/models/agent_iteration.py`
- Fields: id, task_id (FK), action_id (FK), iteration_number, loop_type, attempt_number, reasoning, tool_calls (JSON), outcome, error, lessons_learned, created_at, duration_ms
- Added composite index on (task_id, iteration_number)
- Registered in `models/__init__.py` and `database.py` init_db imports
- Files changed: `backend/app/models/agent_iteration.py` (new), `backend/app/models/__init__.py`, `backend/app/database.py`
- **Learnings for future iterations:**
  - Model file pattern: import uuid, datetime; import SA types; import Base from app.database; define class extending Base
  - The `__table_args__` tuple is used for composite indexes
  - py_compile works for basic syntax/import checking; runtime import test catches SQLAlchemy issues
---

## 2026-02-26 - US-003
- Added `workspace_path` (Text, nullable) and `workspace_branch` (String(200), nullable) fields to Task model
- Added both fields to `TaskResponse` Pydantic schema so they appear in API responses
- Added idempotent `ALTER TABLE` migrations in `init_db()` for existing databases
- Files changed: `backend/app/models/task.py`, `backend/app/schemas/task.py`, `backend/app/database.py`
- **Learnings for future iterations:**
  - Adding new nullable columns is straightforward: add `Mapped[str | None]` to model, add field to response schema, add ALTER TABLE to init_db
  - Existing tasks unaffected since fields default to None
---

## 2026-02-26 - US-002
- Added `GET /actions/{action_id}/tasks/{task_id}/iterations` endpoint to tasks router
- Created `AgentIterationResponse` Pydantic schema with all fields matching the model
- Endpoint returns iterations ordered by `iteration_number`, empty list if none exist
- Validates task exists and belongs to action (404 if not found)
- Files changed: `backend/app/routers/tasks.py`, `backend/app/schemas/task.py`
- **Learnings for future iterations:**
  - Schema pattern: add Pydantic response models to `backend/app/schemas/task.py` with `model_config = {"from_attributes": True}` for ORM compat
  - Router pattern: import models from `app.models` (which re-exports from submodules); import schemas from `app.schemas.task`
  - Existing endpoint patterns: validate parent entity exists first, use `select().where().order_by()`, return `scalars().all()`
---

## 2026-02-26 - US-004
- Implemented git worktree manager service in `backend/app/services/worktree_manager.py`
- Three async functions: `create_worktree(task_id, repo_path)`, `remove_worktree(worktree_path)`, `list_worktrees(repo_path)`
- Worktrees created at `{repo_path}/.workdeck/worktrees/{task_id}/` with branch `workdeck/task-{task_id[:8]}`
- Uses `asyncio.create_subprocess_exec` for git commands (non-blocking)
- `WORKDECK_REPO_PATH` env var configures default repo path
- Handles edge cases: branch already exists, worktree removal fallback, stale worktree pruning
- Files changed: `backend/app/services/worktree_manager.py` (new)
- **Learnings for future iterations:**
  - Use `asyncio.create_subprocess_exec` for subprocess calls in async code (not `subprocess.run`)
  - `git worktree list --porcelain` gives machine-parseable output with `worktree`, `HEAD`, `branch` fields
  - Services don't need to be registered in models/__init__.py — only models do
---

## 2026-02-26 - US-005
- Implemented coding agent tool functions in `backend/app/services/agents/coding_tools.py`
- Six async functions: `read_file`, `write_file`, `edit_file`, `glob_search`, `grep_search`, `bash_run`
- All path-accepting functions use `_resolve_and_validate()` to ensure paths stay within workspace directory
- `edit_file` returns unified diff of the change, replaces first occurrence only
- `grep_search` skips `.git/` dirs and binary files, caps at 500 results
- `bash_run` uses `asyncio.create_subprocess_shell` with configurable timeout
- Files changed: `backend/app/services/agents/coding_tools.py` (new)
- **Learnings for future iterations:**
  - `Path.resolve()` + `relative_to()` is the reliable way to check path containment (handles symlinks, `..`, etc.)
  - `difflib.unified_diff` needs `keepends=True` on splitlines to produce clean diffs
  - `asyncio.create_subprocess_shell` for shell commands vs `asyncio.create_subprocess_exec` for direct execution
  - Tool functions are standalone async functions, not methods on a class — keeps them easy to test and wire into LLM tool schemas
---
